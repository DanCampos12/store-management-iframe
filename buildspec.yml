version: 0.2
env:
  variables:
    - export NODE_ENV=development
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - yarn install --frozen-lockfile
  build:
    commands:
      - yarn build
  post_build:
    commands:
      - aws s3 sync --delete ./dist $S3_BUCKET